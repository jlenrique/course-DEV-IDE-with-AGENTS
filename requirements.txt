# Core dependencies for collaborative intelligence infrastructure
# Install: pip install -r requirements.txt

# HTTP clients
requests>=2.31,<3
aiohttp>=3.9,<4

# Authentication
pyjwt>=2.8,<3

# Configuration & validation
python-dotenv>=1.0,<2
pydantic>=2.7,<3
pyyaml>=6.0,<7
imageio-ffmpeg>=0.5,<1

# Source wrangler (PDF text extraction)
pypdf>=5.0,<6

# Sensory bridges (PPTX slide extraction)
python-pptx>=1.0,<2

# Source wrangler (DOCX text extraction) — Story 27-1
python-docx>=1.1,<2

# Development & testing
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
pytest-timeout>=2.3,<3
ruff>=0.4,<1
