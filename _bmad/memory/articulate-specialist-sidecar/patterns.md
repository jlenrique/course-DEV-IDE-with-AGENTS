# Learned Patterns: Articulate Specialist

Append-only. Default mode writes only.

- Branching fan-out stays maintainable when capped at 2 to 3 decisions per node.
- SCORM validation must include gradebook verification, not just package integrity.
- Accessibility checks are most reliable when mapped directly to WCAG 2.1 AA criteria codes.
- Finalized outputs always require human review sign-off before story closure.
