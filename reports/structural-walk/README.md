# Structural Walk Reports

Operational structural-walk reports live here.

- `standard/` contains current standard-workflow reports plus `history/`
  for migrated legacy fidelity-walk artifacts.
- `motion/` contains motion-workflow structural-walk reports.

Do not treat these generated reports as regression fixtures.
