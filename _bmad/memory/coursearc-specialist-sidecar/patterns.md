# Learned Patterns: CourseArc Specialist

Append-only. Default mode writes only.

- Treat LTI launch validation as a hard gate before SCORM or accessibility signoff.
- Always collect evidence artifacts before marking guidance-ready completion.
- For unsupported interaction types, block and escalate to Marcus instead of improvising.
- Document grade-passback fallback explicitly when LTI grading services are unavailable.
