policy:
  ordering: priority_size_id  # options: priority_size_id | manifest
  batch_size: 2
  max_concurrency: 4
retries:
  max_attempts: 2
  retryable_statuses:
    - 429
    - 500
backoff:
  initial: 1.0
  factor: 2.0
  jitter: 0.1
  max: 30.0
hashing:
  algorithm: sha256
