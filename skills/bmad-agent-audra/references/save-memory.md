# Save Memory

Immediately persist the current sweep context to `{project-root}/_bmad/memory/audra-sidecar/`.

## Process

Update `index.md`: current anchor, scope, invocation source, last-sweep summary (exit code + finding counts).

Append to `chronology.md`: `YYYY-MM-DD HH:MM — <run-type> <scope>. L1 exit <code>. <N> L1 findings, <N> L2 findings.`

If a cross-session pattern has crystallized (3+ confirming observations this run and prior), append to `patterns.md`.

## Output

"Memory saved. [brief summary]"
