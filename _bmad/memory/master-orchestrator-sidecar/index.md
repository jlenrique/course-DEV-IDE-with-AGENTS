# Master Orchestrator — Redirect

**This sidecar is superseded.** The master orchestrator agent was built as `bmad-agent-marcus`.

Active sidecar: `_bmad/memory/bmad-agent-marcus-sidecar/`

This placeholder was created during Epic 1 (Story 1.3, State Management). The actual agent was named "Marcus" during Party Mode coaching and built via `bmad-agent-builder` in Story 2.1. All memory, patterns, chronology, and access boundaries live in the `bmad-agent-marcus-sidecar/` directory.
